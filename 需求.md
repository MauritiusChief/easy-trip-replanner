# **1\. 产品目标**

## **1.1 核心目的**

在旅行途中原计划被打乱时，以尽可能低的操作成本，快速得到一份**当天剩余行程的可执行修订草稿**。

主要处理三类情况：

* 某个地点临时取消  
* 前一地点停留超时  
* 后续存在固定时间、固定地点的预约

天气变化可抽象为地点取消。

交通超时可抽象为前一阶段耗时增加。

## **1.2 产品定位**

产品定位是：

* 旅行前完成较完整的行程安排  
* 旅行中进行计划修补  
* 自动规划只负责生成草稿  
* 最终计划由用户决定和编辑

系统关注的是**计划本身**，不维护一套独立的现实执行状态。

---

# **2\. 行程计划模型**

## **2.1 行程是一条多日连续时间轴**

整趟旅行使用连续时间轴表示。

时间精度为分钟级，实际编辑粒度预计采用：

* 5 分钟  
* 或 10 分钟

## **2.2 每个日历日是独立的规划范围**

虽然整体是一条多日时间轴，自动重排只作用于：

> 当前日历日的剩余计划

不同日期之间不进行联合优化。

地点优先级、开放时间等规划属性也按日历日独立存在。

## **2.3 计划由 Slot 构成**

当前计划中有两种主要 slot：

* 地点 slot  
* 交通 slot

它们共同组成完整时间轴。

---

# **3\. 地点规划能力**

## **3.1 地点具有可调整的停留时间**

每个地点可以设置：

* 最短停留时长  
* 最长停留时长

特殊含义：

* 最短时长为空：允许整个地点被取消  
* 最长时长为空：停留时间没有上限

## **3.2 地点可以成为固定时间锚点**

部分地点具有固定开始时间。

固定意味着：

* 地点不移动  
* 开始时间不移动

它们构成当天计划中的硬约束。

固定开始时间直接作为时间轴锚点使用。

用户负责在计划中预留合理交通时间。

## **3.3 地点具有开放时间约束**

每个地点数据点具有一个开放时间区间。

例如：

`09:00–17:00`

同一个现实地点在不同日历日中视为不同计划数据点。

因此同一地点可以：

* 使用相同经纬度  
* 拥有不同开放时间  
* 拥有不同停留时间  
* 拥有不同优先级  
* 拥有其他不同规划属性

## **3.4 地点具有每日优先级**

每个日历日内部，地点在旅行开始前形成明确排序。

例如：

`A > C > B > D`

该排序用于决定：

* 哪些地点优先保留  
* 哪些地点优先获得更多停留时间  
* 哪些地点优先压缩  
* 哪些可取消地点优先被取消

不同日历日之间不存在统一排序。

---

# **4\. 交通规划能力**

## **4.1 交通独立于地点停留时间**

地点之间的移动使用独立交通 slot 表示。

因此时间轴可以表现为：

`地点 A → 交通 → 地点 B → 交通 → 地点 C`

## **4.2 交通 Slot 依附于目的地**

交通 slot 默认属于其目的地点。

概念上可以理解为：

> 到达该地点之前所需的交通阶段

## **4.3 交通时间由用户建立基准**

用户先手动设置交通时间。

系统根据：

* 出发地坐标  
* 目的地坐标  
* 两点距离  
* 用户输入交通时间

推导出该交通段的隐含速度。

## **4.4 路线变化后自动估算新交通时间**

如果自动重排改变了某地点的前序地点：

1. 重新计算两点距离  
2. 计算得到新的隐含速度  
3. 得到新的交通时间草稿
4. 如果速度变化较大，对用户做出警告

用户随后可以继续手动修改。

---

# **5\. 自动重排能力**

## **5.1 自动重排的职责**

自动重排负责：

> 根据当前被修改后的计划状态，为当天剩余行程生成一个可执行草稿。

典型触发场景包括：

* 删除一个地点  
* 修改某个地点开始时间  
* 缩短或延长某个地点停留时间  
* 插入一个新地点  
* 其他编辑导致后续计划无法继续成立

## **5.2 自动重排只处理当天剩余部分**

规划范围从当前需要调整的位置开始，延伸至当天结束。

当天已经固定的地点继续保持固定。

## **5.3 自动重排优先满足硬约束**

当前总体优先关系为：

1. 固定地点与固定开始时间  
2. 不可取消地点  
3. 地点最低停留时长  
4. 每日地点优先级  
5. 路线合理性  
6. 原时间安排

原有 slot 的具体时间位置本身价值较低。

## **5.4 时间不足时自动产生可行方案**

系统遇到时间不足时，直接生成可行草稿。

处理逻辑概念上是：

1. 优先保留高优先级地点  
2. 优先保护高优先级地点的停留时间  
3. 压缩较低优先级地点  
4. 最低压缩至其最短停留时间  
5. 继续排不下时，取消允许取消的低优先级地点  
6. 根据新的前后地点关系重算交通 slot

## **5.5 地点存在优先于完整停留时间**

对于高优先级地点：

* 首先尽量让该地点继续存在于行程中  
* 其次尽量给予更长的停留时间

因此高优先级地点可以被压缩，同时仍优先于低优先级地点得到保留。

---

# **6\. 路线合理性能力**

## **6.1 系统根据经纬度识别明显绕路**

地点顺序本身没有固定价值。

系统通过空间距离检查路线是否明显不合理。

例如：

`A → B → C`

若：

`distance(A, C) < distance(A, B)`

达到一定程度后，可以认为 B 的当前位置可能产生绕路。

## **6.2 绕路检测用于提示用户**

系统发现潜在绕路时：

* 给出提示  
* 由用户决定是否调整计划

绕路检测本身不直接触发自动修改。

## **6.3 路线合理性参与重排**

当用户主动进行自动重排时，系统应倾向于生成较少绕路的草稿。

路线优化服从更高层级的固定时间与停留约束。

---

# **7\. 备选地点能力**

## **7.1 地点可以关联备选地点**

一个计划地点可以关联一个或多个备选地点。

例如一个午餐地点可以关联：

* 特色餐厅  
* 快餐  
* 更近的简餐

这些地点表达的是：

> 原计划发生问题时，可考虑的替代选择

## **7.2 备选地点属于条件性资源**

备选地点平时不参与正常行程。

单纯出现空档时，不主动拿备选地点填充。

## **7.3 用户决定重排时是否启用备选地点**

触发自动重排前一刻，由用户决定：

> 本次规划是否把关联备选地点纳入候选范围。

因此备选机制属于显式可控的规划辅助能力。

---

# **8\. 旅行中计划编辑能力**

## **8.1 用户直接编辑计划**

现实变化通过修改计划表达。

例如：

* 临时不去某地 → 删除或取消地点  
* 某地点停留超时 → 修改后续时间  
* 提前离开 → 修改相关 slot  
* 临时发现新地点 → 插入地点

不单独记录“现实状态”。

## **8.2 主要编辑方式是表单式精确修改**

用户点击某个 slot 后进入编辑界面。

主要操作包括：

* 修改开始时间  
* 修改停留时长  
* 删除地点  
* 添加地点  
* 修改交通时间  
* 修改固定状态  
* 编辑其他规划属性

拖拽不是主要交互方式。

## **8.3 手动编辑与自动规划相互配合**

主要流程是：

`用户修改计划 → 计划产生空缺或冲突 → 用户触发重排 → 系统生成草稿 → 用户继续编辑`

---

# **9\. 旅行中计划查看能力**

## **9.1 首页突出当前计划状态**

旅行途中打开应用时，最重要的信息是当前时间在计划中的位置。

地点 slot 中显示类似：

> 现在应该在 X  
>  应于 14:30 离开

交通 slot 中显示类似：

> 现在应该正在前往 Y  
>  应于 15:10 到达

## **9.2 当前状态由时间轴自动推导**

系统只需要：

* 当前时间  
* 当前计划

即可判断当前位于哪个 slot。

## **9.3 计划是唯一主要状态**

应用无需额外维护：

* 实际到达时间  
* 实际离开时间  
* 延误分钟数  
* “我还在这里”  
* “我已经离开”

用户需要反映现实变化时，直接编辑时间轴。

---

# **10\. 技术与部署约束**

## **10.1 纯前端应用**

整个应用应可以完全运行在浏览器中。

## **10.2 GitHub Pages 可直接部署**

应用需要满足静态网站部署条件，可直接通过 GitHub Pages 发布。

这意味着当前产品方向天然倾向于：

* 客户端数据存储  
* 客户端规划计算  
* 客户端经纬度距离计算  
* 无服务器依赖的核心功能

目前需求层级可以压缩成一句话：

> **这是一个以“预先排好的旅行时间轴”为基础，在旅行途中通过手动修改和当天局部自动重排，快速恢复一份满足固定时间、停留时长和空间合理性的可执行计划的工具。**

