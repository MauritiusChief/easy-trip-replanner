# **1\. 产品目标**

## **1.1 核心目的**

在旅行途中原计划被打乱时，以尽可能低的操作成本，快速手动调整**当天剩余行程**，恢复一份由用户自行决定的可执行计划。

主要处理三类情况：

* 某个地点临时取消  
* 前一地点停留超时  
* 后续存在固定时间、固定地点的预约

天气变化可抽象为地点取消。

交通超时可抽象为前一阶段耗时增加。

## **1.2 产品定位**

产品定位是：

* 旅行前完成较完整的行程安排  
* 旅行中进行计划修补  
* 系统提供行程信息、编辑入口和冲突提示
* 行程顺序与时间均由用户决定和编辑

系统关注的是**计划本身**，不维护一套独立的现实执行状态。

---

# **2\. 行程计划模型**

## **2.1 行程是一条多日连续时间轴**

整趟旅行使用连续时间轴表示。

时间精度为分钟级，实际编辑粒度预计采用：

* 5 分钟  
* 或 10 分钟

## **2.2 每个日历日是独立的规划范围**

虽然整体是一条多日时间轴，手动重排只作用于：

> 当前日历日的剩余计划

不同日期之间不进行联合调整。

地点优先级、开放时间等规划属性也按日历日独立存在。

## **2.3 计划由 Slot 构成**

当前计划中有两种主要 slot：

* 地点 slot  
* 交通 slot

它们共同组成完整时间轴。

---

# **3\. 地点规划能力**

## **3.1 地点具有可调整的停留时间**

每个地点可以设置：

* 最短停留时长  
* 最长停留时长

特殊含义：

* 最短时长为空：允许整个地点被取消  
* 最长时长为空：停留时间没有上限

## **3.2 地点可以成为固定时间锚点**

部分地点具有固定开始时间。

固定意味着：

* 地点不移动  
* 开始时间不移动

它们构成当天计划中的硬约束。

固定开始时间直接作为时间轴锚点使用。

用户负责在计划中预留合理交通时间。

## **3.3 地点具有开放时间约束**

每个地点数据点具有一个开放时间区间。

例如：

`09:00–17:00`

同一个现实地点在不同日历日中视为不同计划数据点。

因此同一地点可以：

* 使用相同经纬度  
* 拥有不同开放时间  
* 拥有不同停留时间  
* 拥有不同优先级  
* 拥有其他不同规划属性

## **3.4 地点具有每日优先级**

每个日历日内部，地点在旅行开始前形成明确排序。

例如：

`A > C > B > D`

该排序用于决定：

* 哪些地点优先保留  
* 哪些地点优先获得更多停留时间  
* 哪些地点优先压缩  
* 哪些可取消地点优先被取消

不同日历日之间不存在统一排序。

---

# **4\. 交通规划能力**

## **4.1 交通独立于地点停留时间**

地点之间的移动使用独立交通 slot 表示。

因此时间轴可以表现为：

`地点 A → 交通 → 地点 B → 交通 → 地点 C`

## **4.2 交通 Slot 依附于目的地**

交通 slot 默认属于其目的地点。

概念上可以理解为：

> 到达该地点之前所需的交通阶段

## **4.3 交通时间由用户建立基准**

用户先手动设置交通时间。

系统根据：

* 出发地坐标  
* 目的地坐标  
* 两点距离  
* 用户输入交通时间

推导出该交通段的隐含速度。

## **4.4 路线变化后重算隐含速度并警告**

如果用户手动重排改变了某地点的前序地点：

1. 交通 slot 的时长由用户在重排时重新填写
2. 重新计算两点距离  
3. 按新距离与填写的时长重算隐含速度
4. 隐含速度与原基准速度偏差较大时，对用户做出警告

交通时长不由速度反推。用户随后可以继续手动修改交通时长；修改时长时，基准速度按新时长与当前距离重算。

---

# **5\. 手动重排能力**

## **5.1 手动重排的职责**

手动重排为用户提供当天剩余地点的选择与重新填写入口，不进行路线搜索、自动排程、自动压缩停留时间、自动取消地点或自动替换备选地点。

典型触发场景包括：

* 删除一个地点
* 前一地点停留超时
* 修改某个地点开始时间或停留时长
* 插入一个新地点
* 其他编辑导致后续计划需要重新调整

## **5.2 重排范围与锁定内容**

手动重排只处理触发日期当天尚未完成、且当前未在进行中的剩余行程。

以下内容不进入待选卡槽：

* 已结束的地点与交通
* 当前正在进行的地点或前往该地点的交通，以及该地点
* 具有固定开始时间的地点

固定开始时间地点始终保留在时间轴中，开始时间和相对顺序不可在手动重排中修改。它们是用户安排其他地点时的明确参考锚点。

## **5.3 待选卡槽与填写流程**

用户点击“重排当天剩余行程”后，所有可调整的剩余地点以卡片形式进入待选卡槽，等待用户逐一选择。

每张待选卡必须显示原计划信息，供重新安排时参考：

* 原计划开始时间与停留时长
* 原前序地点
* 原到达交通时长
* 地点名称、开放时间、固定状态、停留限制、优先级和其他既有规划属性

用户每选择一张卡片，填写该地点新的开始时间，以及前往此处所需的交通时长。若没有可确定的前序地点，则不创建到达交通 slot，并明确提示该情况。

系统按用户的选择顺序安排地点；每次保存后，根据新的前序地点同步交通的出发地、目的地、开始时间和隐含速度信息。

## **5.4 即时保存与中途退出**

每张卡填写并确认后，修改立即写入正式计划，不使用草案、对比视图、逐项采纳或整份采纳流程。

用户可在尚未选完全部待选卡时退出手动重排：

* 已选择并确认的地点保留已保存的调整
* 尚未选择的地点保留原有位置和时间
* 用户可以在之后再次发起手动重排，继续调整剩余行程

## **5.5 约束提示而非自动决策**

用户填写的开始时间和交通时长统一按既定时间粒度取整。系统应检查并明确提示以下情况：

* 与其他地点或交通时间重叠
* 超出每日可规划窗口
* 不符合固定开始时间
* 超出开放时间
* 停留时间不符合最短或最长限制
* 缺少必要交通
* 因前序地点变化产生的异常速度或明显绕路

上述提示不阻止保存。系统不替用户修改时间、顺序、停留时长或地点去留；最终决定始终由用户作出。

---

# **6\. 路线合理性能力**

## **6.1 系统根据经纬度识别明显绕路**

地点顺序本身没有固定价值。

系统通过空间距离检查路线是否明显不合理。

例如：

`A → B → C`

若：

`distance(A, C) < distance(A, B)`

达到一定程度后，可以认为 B 的当前位置可能产生绕路。

## **6.2 绕路检测用于提示用户**

系统发现潜在绕路时：

* 给出提示  
* 由用户决定是否调整计划

绕路检测本身不直接触发自动修改。

## **6.3 路线合理性用于手动重排后的提示**

用户完成一次手动调整后，系统应根据新的地点顺序检查潜在绕路并提示用户。

路线合理性不改变用户已经填写的时间、顺序或交通时长。

---

# **7\. 备选地点能力**

## **7.1 每天拥有一个备选地点库**

每个日历日拥有一个备选地点库。

库中的每个备选地点都是独立条目，拥有自己的全部规划属性：

* 名称与经纬度  
* 计划停留时长  
* 开放时间区间  
* 最短/最长停留时长  
* 每日优先级  
* 固定开始时间（可选）

每个备选地点链接到当日计划中的某个地点，表达：

> 原计划发生问题时，可考虑的替代选择

一个计划地点可以链接多个备选地点。

原计划地点被删除或取消后，其名下备选条目保留为"未连接"状态，可重新绑定或删除。

备选地点被用户加入正式计划后，对应库条目移除。

## **7.2 备选地点属于条件性资源**

备选地点平时不参与正常行程。

单纯出现空档时，不主动拿备选地点填充。

## **7.3 备选地点由用户手动换入**

备选地点不自动参与手动重排，也不自动填补空档。

用户需要使用备选地点时，主动将其加入当天计划，再通过正常编辑或手动重排填写其时间和交通信息。

---

# **8\. 旅行中计划编辑能力**

## **8.1 用户直接编辑计划**

现实变化通过修改计划表达。

例如：

* 临时不去某地 → 删除或取消地点  
* 某地点停留超时 → 修改后续时间  
* 提前离开 → 修改相关 slot  
* 临时发现新地点 → 插入地点

不单独记录“现实状态”。

## **8.2 主要编辑方式是表单式精确修改**

用户点击某个 slot 后进入编辑界面。

主要操作包括：

* 修改开始时间  
* 修改停留时长  
* 删除地点  
* 添加地点  
* 修改交通时间  
* 修改固定状态  
* 编辑其他规划属性

拖拽不是主要交互方式。

## **8.3 手动编辑与手动重排相互配合**

主要流程是：

`用户修改计划 → 计划产生空缺或冲突 → 用户触发手动重排 → 逐张选择地点并填写时间和交通 → 每次确认立即保存`

---

# **9\. 旅行中计划查看能力**

## **9.1 首页突出当前计划状态**

旅行途中打开应用时，最重要的信息是当前时间在计划中的位置。

地点 slot 中显示类似：

> 现在应该在 X  
>  应于 14:30 离开

交通 slot 中显示类似：

> 现在应该正在前往 Y  
>  应于 15:10 到达

## **9.2 当前状态由时间轴自动推导**

系统只需要：

* 当前时间  
* 当前计划

即可判断当前位于哪个 slot。

## **9.3 计划是唯一主要状态**

应用无需额外维护：

* 实际到达时间  
* 实际离开时间  
* 延误分钟数  
* “我还在这里”  
* “我已经离开”

用户需要反映现实变化时，直接编辑时间轴。

---

# **10\. 视觉与交互原则**

## **10.1 低干扰视觉系统**

界面应以中性、低饱和度的背景、边框和文字建立层级，避免使用装饰性强调色、彩色大面积填充或与任务无关的视觉效果，减少旅行途中编辑计划时的注意力分散。

## **10.2 颜色只表达功能状态**

颜色仅用于需要用户注意或判断的功能状态，例如：

* 时间重叠、开放时间冲突和固定时间冲突
* 表单校验错误、存储失败或不可恢复的问题
* 键盘焦点和当前操作项等必要交互状态

功能状态不能只依赖颜色表达，必须同时提供明确文字、图标、边框或其他非颜色线索。普通地点、交通、空档、按钮、卡片和普通信息不应仅为装饰目的使用颜色区分。

## **10.3 移动端操作**

待选卡、参考信息和时间/交通输入在窄屏设备上应保持易读、可点击和可连续操作；触屏操作目标应足够大，避免依赖拖拽完成重排。

---

# **11\. 技术与部署约束**

## **11.1 纯前端应用**

整个应用应可以完全运行在浏览器中。

## **11.2 GitHub Pages 可直接部署**

应用需要满足静态网站部署条件，可直接通过 GitHub Pages 发布。

这意味着当前产品方向天然倾向于：

* 客户端数据存储  
* 客户端行程计算与警告  
* 客户端经纬度距离计算  
* 无服务器依赖的核心功能

目前需求层级可以压缩成一句话：

> **这是一个以“预先排好的旅行时间轴”为基础，在旅行途中通过手动修改和当天局部手动重排，快速恢复一份由用户决定、并可获得约束与路线提示的可执行计划的工具。**

