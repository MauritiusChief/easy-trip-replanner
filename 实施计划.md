# Easy Trip Replanner 分阶段实施计划

状态：已过时

## 已确认的产品约束

- 应用为纯前端 React + TypeScript + Vite 项目，可静态部署到 GitHub Pages。
- 不接入地图、路线、地理编码或其他外部 API。
- 行程的内部时间计算统一使用 UTC；用户在旅行开始前按所在时区定义每日开始与结束时间，界面按该时区显示和编辑。
- 所有可编辑的时间均按 5 分钟粒度取整。
- 数据仅保存在浏览器 `localStorage`，不提供 JSON 导入、导出。
- 计划是唯一主要状态；不记录实际到达、离开、延误或“已完成”等执行状态。
- 自动规划仅生成草案，必须由用户确认后才替换当前计划。

## 阶段 0：规则定稿与示例场景

### 目标

在开始界面和重排器开发前，确定可实现、可测试的 MVP 规则，并准备贯穿开发与验收的标准行程数据。

### 工作内容

- 明确旅行级配置：旅行名称、显示时区、旅行起止日期，以及每日可规划的开始和结束时间。
- 定义时区转换规则：每日边界及用户输入的本地时间转换为 UTC 保存；渲染和编辑时转换回旅行配置的时区。
- 定义地点与交通的最小数据模型：名称、坐标、开放时间、最短/最长停留时长、固定开始时间、每日优先级、交通基准时长、备选地点关联。
- 将“最短时长为空”定义为可取消，将“最长时长为空”定义为无上限；明确固定地点不可移动且固定开始时间不可变化。
- 默认阈值在代码中集中管理，方便后续调整：绕路比例、速度变化比例、路线候选数量和重排搜索上限。
- 准备覆盖取消地点、前序超时、固定预约、开放时间冲突、交通时长变化、启用备选地点的示例行程。

### 默认参数建议

首版使用以下保守参数，并在实际使用中根据草案质量调整：

- 绕路提示：经过中间点后的距离比直接前往后续地点的距离多出至少 50%，且额外距离不少于 1 公里。
- 速度异常提示：重新计算后的隐含速度与原基准速度相比增加或降低 40% 以上。
- 路线优化：仅在硬约束可满足的前提下，对单个可调整区间的候选地点进行有限排列搜索；地点数量较多时使用贪心近邻策略。

### 验收结果

- 形成可直接编码的领域规则和示例数据。
- 所有时间、约束优先级与不可行状态都有明确的处理定义。

## 阶段 1：基础架构、领域模型与本地存储

### 目标

建立可靠的计划数据基础，确保多日行程可以在刷新页面后继续使用。

### 工作内容

- 清理模板页面，建立应用壳、页面布局和基础视觉规范。
- 建立领域类型：`Trip`、`DayPlan`、`PlaceSlot`、`TransportSlot`、`AlternativePlace`、规划警告和重排草案。
- 实现 UTC 时间、旅行时区、本地日期、5 分钟取整、时长和每日边界的纯函数。
- 建立浏览器数据仓库：首次打开时创建示例行程，后续读取和自动写入 `localStorage`。
- 为本地数据增加格式版本号和安全校验；当数据无效或存储失败时，显示可恢复提示而非让应用崩溃。
- 实现地点间直线距离计算和由距离、用户设定交通时长推导隐含速度的基础函数。

### 验收结果

- 用户可以打开示例行程，刷新页面后数据保持不变。
- 时间可在旅行时区正确显示，内部计划边界和 slot 时间保持 UTC 一致性。

## 阶段 2：行程查看与手动编辑

### 目标

提供旅行中可快速查看、通过表单精确修改的时间轴。

### 工作内容

- 实现按日期切换的连续时间轴，展示地点 slot、交通 slot、空档、每日边界和固定时间锚点。
- 根据当前 UTC 时间和计划时间轴推导当前 slot，并按旅行时区显示“现在应该在 X”或“现在应该正在前往 Y”。
- 实现地点编辑表单：名称、坐标、开放时间、停留时长范围、优先级、固定状态、固定开始时间。
- 实现日级备选地点库（需求 7）：堆叠卡片视图与独立属性编辑，条目链接到当日计划地点；链接目标被删除后条目保留为"未连接"。
- 实现交通编辑表单：目的地点关联、基准交通时长及关联的速度信息。
- 实现地点插入、删除或取消、时间修改、时长修改和固定状态修改。
- 在保存编辑时校验时间粒度、每日边界、开放时间、固定锚点、slot 重叠和交通缺失，并明确显示冲突而非隐式修改计划。
- 在编辑造成空档或冲突时，展示“重排当天剩余行程”的明确入口。

### 验收结果

- 用户能创建和维护包含地点、交通、固定预约与备选地点的多日计划。
- 首页能准确显示当前计划位置，编辑操作不会额外维护现实执行状态。

## 阶段 3：当天局部自动重排引擎

### 目标

在用户主动触发时，对当前日历日的剩余计划生成满足约束的可执行草案。

### 工作内容

- 将重排逻辑设计为独立的纯计算模块，输入为当天计划、重排起点、是否启用备选地点和规划参数，输出为草案、取消项、警告与不可行原因。
- 锁定重排起点之前的计划以及所有固定开始时间地点；不跨越当天边界，也不修改其他日历日。
- 按以下优先级生成排程：固定地点与固定开始时间、不可取消地点、最低停留时长、每日地点优先级、路线合理性、原有具体时间位置。
- 在时间不足时，优先保留高优先级地点；优先压缩低优先级地点至最低时长；仍无法安排时取消允许取消的低优先级地点。
- 对每个因顺序变化而改变前序地点的交通段，保持交通时长不变，按新距离重算隐含速度并与基准速度比较，偏差超过阈值时添加警告；交通时长由用户手动调整。
- 只在用户本次选择启用时，将关联备选地点加入候选集合；备选地点不用于主动填补普通空档。
- 计算潜在绕路并作为提示显示；绕路检测本身不自动修改未触发重排的计划。

### 验收结果

- 取消地点、延长停留和插入地点后，用户可为当天剩余行程得到独立草案。
- 草案不违反固定开始时间、不可取消地点、最低停留时长、开放时间和每日边界；无法满足时可说明具体原因。

## 阶段 4：草案对比、确认与移动体验

### 目标

确保自动结果可解释、可控，并适合旅行途中在手机上操作。

### 工作内容

- 提供原计划与重排草案的对比视图，突出顺序、时间、停留时长、交通、取消地点和备选地点的变化。
- 显示绕路、速度异常、开放时间边界和不可行约束等警告的原因。
- 提供整份采纳、逐项采纳、放弃草案和返回编辑的操作；未确认的草案不写入正式计划。
- 适配窄屏视图，保证时间轴、当前状态、slot 编辑和重排操作在移动设备上易于使用。
- 完善键盘操作、焦点管理、表单错误关联和足够的颜色对比度。

### 验收结果

- 用户能够理解自动重排为什么改变计划，并能保留自己的最终决定权。
- 核心查看、编辑、重排和确认流程在桌面端与移动端均可用。

## 阶段 5：测试、调参与发布

### 目标

保证核心规则可回归验证，并完成 GitHub Pages 发布准备。

### 工作内容

- 为时间处理、时区转换、距离、速度、数据校验和 `localStorage` 仓库添加单元测试。
- 为重排器添加场景测试：固定锚点、开放时间、最短/最长停留、不可取消地点、跨日隔离、地点取消、交通重算和备选地点启用。
- 为关键用户流程增加浏览器级测试：初始示例数据、手动编辑、刷新持久化、生成草案、采纳和放弃草案。
- 使用阶段 0 的标准行程反复评估绕路与速度警告的误报、漏报，并调整集中配置的默认阈值。
- 执行 `npm run lint` 和 `npm run build`，验证 GitHub Pages 子路径下的静态资源加载与部署工作流。

### 验收结果

- 核心规划规则有自动化回归保护。
- 应用无需服务端或外部 API，即可构建并部署到 GitHub Pages。

## 建议实施顺序

优先完成阶段 0 和阶段 1，再以阶段 2 的真实编辑结果校验数据模型。阶段 3 应在手动编辑闭环稳定后开发，避免重排器建立在未经验证的计划结构上。阶段 4 和阶段 5 可在重排器具备基础可用性后并行推进。
