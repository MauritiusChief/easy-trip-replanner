# Easy Trip Replanner 手动重排重构计划

## 目标与边界

本计划将当前“自动计算重排草案，再由用户采纳”的实现替换为“用户逐张选择当天剩余地点，并即时填写时间和通勤”的手动重排流程。同时收敛界面色彩，使颜色只表达警告、错误、焦点和当前操作等功能状态。

本次重构的边界：

- 保留 React、TypeScript、Vite、Zustand、`localStorage` 和现有 `Trip` 数据结构。
- 保留地点、交通、备选地点、时区、5 分钟粒度、当前状态推导和既有表单编辑能力。
- 重排不执行路径搜索、自动排序、自动压缩、自动取消或自动替换备选地点。
- 固定时间地点、已结束地点和当前正在进行的地点不进入待选卡槽。
- 冲突只产生可理解的警告，不阻止用户保存。
- 不迁移 `localStorage`：自动重排草案从未持久化，移除其临时类型不会影响已存行程。

## 当前基线

自动重排目前跨越多个层次，不能只替换按钮文案：

- `src/domain/replan.ts` 计算 `ReplanDraft`，包含候选排序、固定锚点、压缩、取消和备选替换。
- `src/domain/types.ts` 定义 `ReplanDraft`，`src/domain/draftDiff.ts` 与 `src/ui/DraftCompare.tsx` 消费其差异和采纳逻辑。
- `src/domain/mutations.ts` 的 `withLegReplaced`、`withDraftAdopted` 服务于草案采纳。
- `src/state/tripStore.ts` 保存 `draft`，并公开生成、采纳、放弃草案的操作。
- `src/ui/DayView.tsx` 提供自动重排开关、备选地点开关、草案对比和召回入口。
- `src/index.css`、`DayView.css`、`DraftCompare.css` 等将紫色强调色用于大量普通卡片和流程层级。

## 阶段 0：规则收口与回归基线

### 工作内容

- 将手动重排的范围判定提炼为可测试规则：已结束和进行中的段落构成锁定前缀；固定开始时间地点是锁定锚点；其余地点进入待选卡槽。
- 明确“用户选择顺序”与“时间轴顺序”的规则。建议以用户填写的开始时间作为正式时间轴顺序；选择顺序仅决定用户正在处理的卡片和下一张卡的默认前序参考。开始时间相同或倒置时稳定保留选择顺序，并显示重叠或顺序警告。
- 明确首张可调整卡没有可确定前序地点时不创建交通 slot；有锁定前缀时，以锁定前缀最后地点为前序。
- 明确固定锚点相邻的可调整地点保存后仍由时间和警告表达冲突，不自动移动锚点或其他地点。
- 准备最小的标准行程数据：无锁定前缀、存在当前地点、存在固定锚点、存在已完成地点、时间重叠、开放时间冲突、异常速度和中途退出。
- 选择并配置测试工具。建议引入 Vitest，以覆盖领域函数和 mutation；现有项目没有测试命令或测试文件。

### 验收标准

- 每种地点都能被明确归类为锁定、待选或正常编辑对象。
- 时间排序、同一开始时间和用户选择顺序的关系有唯一规则，实施时不依赖临时判断。
- 标准数据可复现本计划中的所有关键场景。

## 阶段 1：建立手动重排领域操作

### 工作内容

- 新建独立的手动重排领域模块，提供“计算锁定前缀与待选地点”的纯函数；复用当前 `resolvePrefix` 的语义，不保留自动排程算法。
- 定义仅存在于 Zustand/UI 内存中的 `ManualReorderSession`：目标日期、锁定范围、待选地点 ID、已处理地点 ID、当前卡片和必要的选择顺序。它不写入 `Trip` 或 `localStorage`。
- 定义手动保存输入：地点 ID、新开始时间、到达交通时长，以及由会话推导的前序地点。
- 实现一个原子 mutation。它必须在一次更新中：
  - 更新被选地点的开始时间和交通时长。
  - 根据新的前序地点同步该段交通的 `from`、`to`、`start` 与隐含速度基准相关信息。
  - 同步受影响后续交通段的出发地快照。
  - 以开始时间稳定排序当天 `legs`，确保数组顺序仍与时间轴、当前位置推导和警告逻辑一致。
  - 保留地点属性、未选择地点、固定锚点和备选库关联。
- 复用 `collectDayWarnings`，在每次 mutation 后重新计算并在界面展示警告，而不是将其作为保存前置条件。

### 验收标准

- 单次保存不会创建草案，也不会丢失任何未选择地点。
- 改变地点前序后，关联交通的坐标快照和时间一致。
- 已选地点立即进入正式行程；退出会话后未选地点仍保留原时间和位置。
- 行程段数组始终符合按时间稳定排序的既有约定。

## 阶段 2：实现待选卡槽与即时保存界面

### 工作内容

- 将 `DayView` 中“重排当天剩余行程”入口替换为“开始手动重排”，启动当天的 `ManualReorderSession`。
- 展示待选卡槽，并为每张卡显示原计划开始时间、停留时长、原前序地点、原交通时长，以及开放时间、优先级和停留限制等参考信息。
- 点击待选卡后打开专用输入界面或复用编辑抽屉的专用模式，要求填写新的开始时间和到达交通时长；输入按 5 分钟粒度解析和校验。
- 保存操作调用阶段 1 的原子 mutation，关闭当前输入，移除该卡的待选状态，并即时刷新时间轴和警告。
- 提供“退出手动重排”操作：仅关闭会话 UI，不回滚已保存的变更；未选择卡片继续以原计划保留。
- 固定开始时间地点保留在时间轴中并提供锁定说明，不显示于待选卡槽。
- 在移动端优先保证卡槽、参考信息、时间输入、交通输入、保存和退出操作可见且易点击；不引入拖拽作为必要操作。

### 验收标准

- 用户可从当天卡片发起重排、依次处理任意待选卡，并在每次确认后立刻看到正式时间轴更新。
- 原计划信息在填写前可见，用户无需记忆原时间或交通时长。
- 退出后重新进入不会丢失已保存调整，未选地点仍可继续编辑或再次重排。
- 固定锚点和当前进行中地点不能被误选。

## 阶段 3：移除自动重排与草案路径

### 工作内容

- 从 `tripStore` 移除 `draft` 状态及 `runReplan`、`applyDraftItem`、`adoptDraft`、`discardDraft` 操作；普通编辑不再承担“使草案失效”的职责。
- 删除 `ReplanDraft` 类型、`src/domain/replan.ts`、`src/domain/draftDiff.ts`、`src/ui/DraftCompare.tsx` 及其样式。
- 删除仅用于草案采纳的 mutation，如 `withDraftAdopted`；评估 `withLegReplaced` 是否仍有独立编辑用途，无用途则删除。
- 从 `DayView` 删除备选地点纳入开关、草案对比、草案失败卡和草案召回横幅。
- 清理所有与自动规划、草案、采纳、取消候选和自动替换有关的注释、类型导入、文案与死代码。
- 保留备选地点库；其换入计划应走明确的手动添加和编辑流程，不再是重排候选。

### 验收标准

- 项目中不存在可达的自动排程、草案生成、草案对比或采纳入口。
- TypeScript、构建和 lint 均不引用已删除的类型或模块。
- 手动重排和普通地点/交通/备选地点编辑均可独立保存。

## 阶段 4：收敛为功能性色彩系统

### 工作内容

- 在 `src/index.css` 建立语义化中性色令牌：页面背景、表面、主/次级文字、边框、悬浮层和焦点轮廓。
- 保留有限状态令牌：警告、错误、冲突和可访问的焦点状态。删除将强调色用于普通地点卡、草案区域、常规按钮或装饰性背景的规则。
- 将普通地点、交通、空档、备选库、待选卡和按钮改以排版、间距、边框、图标和状态文字区分；颜色不再是其主要身份标识。
- 对时间重叠、开放时间冲突、固定时间冲突、输入错误和存储失败使用一致的状态样式，并始终搭配文本、图标或边框。
- 删除 `DraftCompare.css` 后，检查 `DayView.css`、编辑器样式、状态条和页头中遗留的共享类依赖；将真正共享的样式集中到合适位置，避免跨组件隐式依赖。
- 在浅色和深色模式下检查正文、按钮、警告文本和焦点轮廓的对比度，尤其避免低对比度的浅色文字配强调背景。

### 验收标准

- 普通内容使用中性视觉层级，页面不存在仅为装饰而使用的高饱和颜色块。
- 每个功能性状态在不辨色条件下仍可通过文字、图标或边框理解。
- 桌面与窄屏下卡片、输入框和按钮均保持可读、可操作。

## 阶段 5：测试、回归与发布检查

### 工作内容

- 为锁定范围、待选集合、时间粒度、稳定排序、交通拓扑重建、异常速度和警告收集添加单元测试。
- 添加手动重排场景测试：锁定前缀后的首张卡、多个连续选择、固定锚点、时间重叠仍保存、开放时间冲突仍保存、首张无前序地点、中途退出和刷新后的持久化。
- 添加浏览器级流程测试：发起手动重排、查看原计划参考、填写时间与通勤、立即保存、退出、重新进入，以及移动视口下操作。
- 执行 `npm run lint`、测试命令和 `npm run build`；验证 GitHub Pages 子路径构建结果。
- 手工检查浅色/深色模式、键盘焦点、屏幕阅读器标签和窄屏布局。

### 验收标准

- 自动重排删除后，手动重排的关键规则有自动化回归保护。
- 构建、lint 和测试全部通过。
- 刷新页面后，已即时保存的调整仍存在；未保存的会话 UI 不会被恢复为正式行程数据。

## 建议实施顺序

先完成阶段 0，解决选择顺序与时间轴顺序的唯一规则；随后实施阶段 1 和阶段 2，使手动流程在不移除旧路径时即可独立验证。阶段 3 在手动流程稳定后一次性删除自动路径，阶段 4 可与阶段 3 并行推进，最后以阶段 5 完成回归、可访问性和部署验证。

## 完成定义

满足以下条件时，本次重构完成：

- 用户不再接触自动重排、草案或采纳流程。
- 用户可按卡槽逐张重排当天剩余地点，并参考原计划信息填写时间和通勤。
- 每次确认立即保存，退出不回滚，冲突只提示。
- 固定时间、当前进行中和已完成行程不会被重排。
- 普通界面以中性色为主，颜色仅承担功能状态表达。
